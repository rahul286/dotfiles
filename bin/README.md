This folder contains scripts (mostly bash).

These will be added to PATH so I can run them directly from anywhere.

If I do not want any of my script to appear everywhere, automatically, I will put them inside `scripts` folder instead.
